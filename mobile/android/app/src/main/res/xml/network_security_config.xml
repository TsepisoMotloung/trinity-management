<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Trinity Sound Mobile.
    - Release: only HTTPS allowed (certificate pinning can be added)
    - Debug: cleartext allowed for local development (10.0.2.2)
-->
<network-security-config>
    <!-- Production: HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to Android emulator host (dev only) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
